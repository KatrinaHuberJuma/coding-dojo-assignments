pip freeze is used to create requirements.txt to keep 
track of versions/dependancies for future use. 
Requirements.txt is used to recreate the exact 
environment in which the program was created and made to work.

install django and uninstall django are self explanatory

show django checks that uninstall was successful

search flask shows all packages with names or summaries containing "flask" 